## Datasets
This folder contains the data used for the project.\
The `tacred` folder contains the TACRED dataset, while the `tacrev` folder contains the revisited version of the data, with relabelled test and dev dataset.

Both folders are structured in the same way. They contain two folders `conll` and `json` containing the respective formats.